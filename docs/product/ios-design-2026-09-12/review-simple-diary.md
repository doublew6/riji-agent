# 打开即日记：结构与措辞审查

日期：2026-09-12。对象：`docs/PRD.md` v2.2-review，以及低保真生成器末尾产品统一服务覆盖后的界面与状态。本审查帮助用户确认直接写今天的体验，并帮助实现者保留账号边界、真实保存与原日记流程。依照已应用的 BMad 文档标准先结构后措辞；不改主稿、原型或补充说明，不新增平台研究。

重点核对 FR-01–05、09、13–15、34、36、38，B01/A01/C01/C03/C07/D01/D03，A02–A07 与 H12/H13。下面仅列截至复读时仍待处理的 3 项；主任务已经修正的 FR-30/33“本地”语义、普通用户重新配对及记忆权限文案不重复计入。

## 结构先行

主稿采用 Strategic/Context（Pyramid），低保真按正常使用顺序展开。**未发现需要再搬动章节或增加页面的结构问题。** 保留“今天正文及页内状态 → 条件分支 → 目录与原流程 → 按需账号 → 维护者高级页”的顺序；102 个状态不代表 102 个必经页面。B01 与 A01 均直接可写，A02–A07 已说明回原请求位置，H12/H13 的角色隔离及服务端验证应保留。

FR-14 的受限写入规则、FR-15 的本机/服务端状态区分、FR-38 的同日碰撞与跨午夜处理应保留。它们与简化前台页面并不冲突。保留直接、中文产品含义为主的措辞，不把实现协议重新放回正常日记页面。

## 剩余措辞与状态表达

| Pass | Original Text | Revised Text | Changes |
| --- | --- | --- | --- |
| prose | **高 · FR-14：** `若服务端实际预览不同于本人所见，或目标/原文/来源改变，才重新展示差异确认`。今日流程说明：`只有 AI 改动才展开预览。` D01 状态：`仅 AI 改动或目标变化时需预览`。 | FR-14 改为：`草稿过期、授权失效、服务端实际预览与本人所见不一致，或目标/原文/来源改变时，重新核对并确认；不得沿用旧确认。` 流程说明改为：`普通原文在输入区确认；AI 改动或确认失效时，在同页重新核对。` D01 状态改为：`AI 整理结果待确认；失效预览经 D05 重新核对`。 | “才/只有/仅”将重验条件缩成内容变化，会与前句 30 分钟时效、FR-16 重连重验及 D05 冲突。沿用现有失效状态，无需增加正常步骤。 |
| prose | **中 · D01 → D03：** D01 确认内容为 `散步时，我想试试先用两句话留下当天的想法。`；点击采用保存后，D03 固定显示 `今天散步时想到，先把想法写成两句话。`，与直接原文保存共用同一正文。D03 注释虽说明保持 AI 标识，手机示意没有展示本次路径的实际结果。 | D03 按原操作展示实际确认内容；整理路径显示 `散步时，我想试试先用两句话留下当天的想法。`，直接原文路径保留原话。屏外说明补为：`本状态继承原操作的日期、区块、实际确认文字和来源类型；AI 导师结果保留其专属标识，不能借此状态改成普通本人正文。` | 走查必须看见“保存的是刚才确认的文字”；不能让固定成功示例把整理结果变回原文，或淡化共用状态的来源类型。可在现有状态中做条件示例，不必新增成功页。 |
| prose | **中 · A8：** `原日记功能必须保留，具体模板、北极星口径与原自动化从本人配置迁入`；§8 P0-B 门槛：`原模板/规则取得并逐项对照；精确周期、来源和受控保存验证，不用声明代替实现`。 | A8 改为：`已有用户保留并迁入原模板、本人北极星口径与原自动化；新用户使用产品初始模板，北极星由本人填写，日常任务按本人设置开启。` P0-B 门槛改为：`新用户验证产品模板与本人设置；迁移用户另取原模板/规则逐项对照。共同验证精确周期、来源与受控保存，不用声明代替实现。` | FR-01/37/45 已明确两类用户，但汇总默认与阶段门槛仍统一写为个人迁移，容易让实现者继续把“找到外部配置”作为新用户使用前提。此处只是对齐既有分支，不增加默认指标或自动任务。 |

计量使用技能自带 `word_metrics.py`，复读快照主稿为 **14,946** 计量词；§3 为 **1,405**，§4.1 为 **650**，§4.3 为 **619**，§4.4 为 **694**，§4.8 为 **567**，§4.9 为 **696**，§8 为 **425**，§10 为 **585**。规则为 CJK 字符加其余空白分词。结构调整建议 0 项，预计结构删减 **0 词 / 0%**；无长度目标，也不为压缩文字牺牲状态与权限含义。上述 3 项是局部表达修正。

主任务处理结果：三项已全部应用。FR-14/D01/流程说明补过期与授权失效重验；D03 改为明确的实际确认文字占位并保留原类型，避免固定回显原话；A8/P0-B 区分新账号初始模板和既有用户迁移。

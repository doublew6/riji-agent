# iOS PRD 与补充说明：措辞审查

日期：2026-09-12。在[结构审查](review-structure.md)之后执行；已核对有界摘录段移至 FR-24。本审查只给出措辞建议，未修改 PRD 或补充说明。

这组文档帮助用户理解并走查 iOS 日记体验，也帮助后续实现者准确识别权限、保存与恢复契约。

依据 `editorial-common.md` 与 `lens-prose.md`，保留文档直接、具体的中文表达，保留“本人”“日记”“记忆”“问题”等产品用语、必要的技术名称和明确的状态区分。忽略代码块、frontmatter 和结构标记；不因偏好改写句子，不新增功能。结构审查没有 CUT 项，保留项不作删减。

**结论：建议 5 处局部澄清，均可在现有结构内修改。** 主要避免把权限校验读成每次重新授权，把内容校验摘要读成 AI 摘要，以及让 VPN 共存成为隐含承诺。

| Pass | Original Text | Revised Text | Changes |
| --- | --- | --- | --- |
| prose | PRD / FR-25：`个人来源模式在后端逐次授权、撤权、缓存失效未通过验收前禁用；` | `个人来源模式在后端尚未通过逐次权限校验、撤权生效和缓存失效验收前禁用；` | 明确每次处理要校验已有授权，不是要求用户每次重新授权；同时补齐三个验收对象的语法关系。保留既定门禁。 |
| prose | 补充说明 §6：`同步清单以受控文件标识、内容版本/摘要、类型、修改时间与附件关系描述文件。内容摘要用于检测变化，不授权上传内容；` | `同步清单以受控文件标识、内容版本、校验摘要（哈希）、类型、修改时间与附件关系描述文件。校验摘要只用于检测变化，不代表上传内容的授权；` | 将技术上的内容校验值与后文工作背景“摘要”区分，避免实现者或产品读者误以为同步需要先生成 AI 摘要。 |
| prose | PRD / FR-24：`达到范围上限时应提示“背景待整理”，由本人明确发起；`；`摘要待更新或来源失效时显著提示，不能用旧摘要伪装最新理解。` | `达到范围上限时应提示“背景待整理”，由本人明确发起整理；`；`背景摘录待更新或来源失效时显著提示，不能把旧摘录显示为最新理解。` | 补齐“发起”的对象，并在同一 FR 中统一指向有界背景摘录；不暗示自动语义压缩。与上一轮移位后的 FR-24 对应。 |
| prose | PRD §10 / A6：`实现者验证 Tailscale 与现有手机 VPN 共存、APNs条件；` | `实现者验证 Tailscale 与现有手机 VPN 切换时的冲突及恢复，并核对 APNs 条件；` | 既有平台研究已说明多 VPN 冲突。“验证共存”容易被读成承诺同时运行；改为本稿已要求的切换、故障及恢复验证。 |
| prose | 补充说明 §9：`AI 请求拿到 Air 持久化回执后，Air 承接执行；` | `Air 持久化任务后返回接收回执，并负责后续执行；` | 明确持久化、返回回执与执行的主体；回执在网络中丢失也不改变 Air 已接收任务的事实，后文未知结果核对规则保持不变。 |

计量沿用技能自带 `word_metrics.py`，重新获取了总量与各标题正文；本轮快照为 PRD **9,651**、补充说明 **5,829** 计量词。计量方式为 CJK 字符加其余空白分词，非自然中文词数。所涉 PRD §4.6 为 913、§10 为 469；补充说明 §6 为 632、§9 为 423。措辞建议以消除歧义为目的，不设缩短目标。

除这 5 项外，未发现需要在本轮修复的理解障碍。不对功能范围、平台实现或模型行为作新的判断；未确认设计仍由主 PRD 的 A1–A7 和实施门槛管理。

主任务处理结果：以上 5 处措辞建议已于 2026-09-12 全部应用。

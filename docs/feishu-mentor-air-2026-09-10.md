# 飞书导师接入 Air 交付记录

2026-09-10，对应 [Issue #43](https://github.com/doublew6/riji-agent/issues/43) 与[飞书实施设计](architecture/feishu-mentor-integration.md)。账号绑定、持久接收及原入口分派基础代码已部署；四个独立导师应用已在飞书后台发布，四个正式接收器已安装运行，四位均已完成真实双端绑定，并通过固定虚构问题的私聊回复验收。

## 基础代码已部署

- 网页与目标飞书私聊的双端身份绑定，保留原记忆 owner；Review token 不进入飞书。
- 每应用独立接收队列、固定投递标识、发送结果记录；重启不盲目重做不确定请求。
- 绑定凭据只在内存处理，重启后重新生成；普通接收队列容量及保留时间有上限。
- 原 Riji 可显式保留 Hermes 接收并分派讨论命令，新接收器禁止接管原应用；该配置尚未在生产启用。
- 修正处理被中断时错误返回“重复成功”的情况，保留群能力门槛和已有来源隔离。

主服务已保留原五个 local 应用、既有唯一 owner 和 Review 凭据，并加入四个 Feishu 导师应用。本人认证后可在网页看到四个导师均为“已连接”；原日记导师继续使用既有 Hermes 入口。后台发布、接收器安装与双端身份绑定分别记录。

## 基础代码部署与验证（历史记录）

通过既有 `air` SSH 核验 MacBook Air。逐文件比较部署前哈希，未发现其他任务对待替换源码的修改；执行 rsync dry-run 后，在 Air 私人部署备份中保存源文件、服务定义和前后哈希清单，再定向同步；具体备份位置只保存在私人运维记录中。

未覆盖 `.env`、应用凭据、SQLite、日志或 vault，未改变模型及记忆 provider，未启动或重启共享容器。8765 的原 Riji 服务 PID 与 runtime 工作目录核对后，才通过生产 executable 重启。38880/38881 的既有 SSH 转发未改动。

| 检查 | 结果 |
| --- | --- |
| 开发机最终完整回归 | 957 项，953 通过、4 跳过、0 失败/错误 |
| Air 生产 Python 相关回归 | 172 项全部通过，无跳过 |
| 网页 JavaScript 语法 | 通过 |
| 代码及文档隐私扫描 | 通过，未发布源码 commit/push |
| production doctor / Hermes bridge status | 通过 |
| `/healthz` / 导师页面 / Memory Review | HTTP 200 |
| 未认证导师 API / 原入口鉴权 | 拒绝未认证访问，旧群入口仍返回 403 |
| 已认证连接 API | HTTP 200、`no-store`；飞书应用列表为空，符合当时配置 |

开发机的四项跳过需要另行配置官方 Codex CLI 测试环境。测试使用合成夹具；生产 HTTP 探针未发起模型调用，也未发送真实日记或记忆。

本次没有重复进行模型质量测试或关闭演练；相关本地版既有证据见[前次交付记录](mentor-local-air-2026-09-10.md)。这不替代本次飞书应用启用后的真实验收。

## 四导师发布与正式接收器安装

四个应用显示名已统一为“日记导师·温柔回顾者”“日记导师·直率教练”“日记导师·未来的我”“日记导师·王阳明”，`1.0.1` 版本均已发布、审核通过。各版本保存前均在可用范围弹窗核验为部分成员且恰好选中本人；外部私聊及对外群能力关闭。机器人能力、长连接事件接收和 `im.message.receive_v1` 已配置，每个应用仅有 `im:message.p2p_msg:readonly`、`im:message:send_as_bot` 两个 scope。

官方 tenant token API 与 `GET bot/v3/info` 四应用均返回 HTTP 200、`code=0`、`activate_status=2`，并匹配新的完整显示名。四份可信测试私聊证明已取得、租户一致并已导出至私密记录；临时探针全部停止，剩余进程为 0。测试消息未被交给模型或日记，也未用于自动认领身份。

正式配置及四个接收器安装已完成。四个任务均运行，已逐个核对 PID、命令参数、工作目录、应用锁和 `0600` spool；每个任务观察到一条 ESTABLISHED TCP 连接，安装就绪时日志和收件均为 0。TCP 连接只证明网络连接存在，不能替代 SDK 认证或真实私聊收发验收。

production doctor 通过，使用既有 production executable 重启主服务后，`/healthz` 与导师页面均为 HTTP 200；未认证 connections 返回 401，本人认证后可见四个应用。安装前核验数据库只有一个 principal，匹配唯一配置 owner；当时四个新应用记录齐全，私聊/外部身份绑定、已确认绑定请求及已绑定 owner 数均为 0，没有他人的历史绑定。绑定后 Air 只读 API 显示四项 connected=true；数据库保持唯一 owner，四份外部身份绑定、四份私聊绑定及四份已确认链接映射完全一致。owner 配置、account 与 legacy_owner 均与安装前备份一致；待确认请求为 0，无额外应用、账户或私聊绑定，共享记忆归属一致已核验。原五个 local 应用、owner 和 Review 凭据保留，Hermes 与 Mem0 未重启，群能力门槛未放开。安装核验未主动发送消息或调用模型。

App Secret 及应用凭据只保存在 Air 的 `0600` 私密文件中，不进入聊天、仓库或本文。

| 本轮与独立准备检查 | 结果 | 证据范围 |
| --- | --- | --- |
| 本轮开发机完整回归 | 957 项：953 通过、4 跳过 | 当前代码回归 |
| 本轮 Air 相关回归 | 154 项全部通过 | 与上节历史 172 项分开记录，不相加或替代 |
| 身份探针准备检查 | 开发机与 Air 均为 61 项全部通过 | 独立合成检查，真实证明另行记录 |
| 安装材料准备检查 | 开发机与 Air 均为 34 项全部通过 | 独立合成检查，正式安装状态见上文 |

## 真实私聊验收与剩余项

四位导师均已通过真实本机 Feishu 用户客户端完成绑定命令发送，由对应机器人实际返回 8 位核对码，再由同一 owner 在已认证网页确认；网页四项均显示“已连接”。绑定通过双端确认完成，没有凭首条测试消息自动认领。

随后向四位导师分别发送同一固定虚构的读书拖延问题；客户端已逐一确认四个正确应用均返回“已收到新问题”和各自独立导师答案。这证明本次真实私聊绑定、接收及回复链路可用，不代表完整质量评估或记忆隔离实测通过。

共享记忆 owner 归属一致已核验；真实内容共享、独立共享事实、私聊隔离及重启恢复验收尚未完成。原 Riji 新增讨论入口、S4 合成群 FG-01..05 与手机端验收也继续单列。群个人内容路径保持关闭，不以后台发布、安装运行或 TCP 状态认定产品验收完成。

## 回滚

若代码发布出现回归，先比对本次备份的 `manifest.json` 与当前源码哈希，确认没有后续修改，再按 `source/` 中的原路径恢复被替换文件。新增模块只有在回退后的引用关系核对完成后才移除；不要批量删除目录或覆盖后来发布的源码。使用同一 production executable 重启并复核健康状态。凭据和真实记忆未迁移，无需恢复其他任务的旧数据库或环境文件。
